/**
 * @fileoverview TSL (Three.js Shading Language) re-exports
 *
 * Convenience re-export for TSL imports.
 * Only available in WebGPU builds.
 *
 * Usage in components:
 *   import { uniform, vec3, Fn } from '#three/tsl'
 */

export * from 'three/tsl'
