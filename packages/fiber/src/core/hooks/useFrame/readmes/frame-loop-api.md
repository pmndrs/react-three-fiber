# Frame Loop Utilities

`invalidate`, `advance`, and the deprecated `addEffect` / `addAfterEffect` / `addTail` globals.

```ts
import { invalidate, advance } from '@react-three/fiber'
```

📖 **Full documentation:** https://docs.pmnd.rs/react-three-fiber/frame-loop#demand-mode

> Design notes for contributors live in `docs/development/frame-loop-design.md`.
