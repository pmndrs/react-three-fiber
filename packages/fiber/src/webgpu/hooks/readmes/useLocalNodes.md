# useLocalNodes

Component-local TSL nodes that rebuild when uniforms, nodes, or textures change.

```ts
import { useLocalNodes } from '@react-three/fiber/webgpu'
```

📖 **Full documentation:** https://docs.pmnd.rs/react-three-fiber/webgpu/tsl-hooks#uselocalnodes
