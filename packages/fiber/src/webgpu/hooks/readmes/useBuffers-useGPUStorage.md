# useBuffers & useGPUStorage

Experimental GPU compute storage: buffer data (`useBuffers`) and storage textures (`useGPUStorage`).

```ts
import { useBuffers, useGPUStorage } from '@react-three/fiber/webgpu'
```

📖 **Full documentation:** https://docs.pmnd.rs/react-three-fiber/webgpu/compute
